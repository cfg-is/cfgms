# Epic: v0.6.0 - Finalizing Foundational Endpoint CMS

**Epic ID**: v0.6.0
**Type**: Major Milestone / Release Epic
**Status**: 📋 Planned
**Story Points**: 60 points (5 stories)
**Priority**: HIGH - Completes foundational endpoint CMS capabilities
**Created**: 2025-10-10

---

## Executive Summary

v0.6.0 completes the foundational endpoint configuration management system by implementing advanced script execution, compliance-focused configuration templates, enterprise-grade Windows patching with major version upgrades, and production-ready performance monitoring for both endpoints and controller infrastructure. This release transforms CFGMS from core platform (v0.5.0) into a fully-featured, production-ready endpoint configuration management system capable of enterprise-scale MSP operations with CMMC/CIS compliance support.

**Core Value Proposition**: Policy-driven automation where MSPs define configurations declaratively and CFGMS continuously enforces them automatically.

---

## Epic Goal

Deliver production-ready endpoint management capabilities that enable MSPs to:
1. Execute scripts safely across multi-platform fleets with DNA-based parameterization
2. Apply CIS/CMMC compliance templates automatically
3. Manage Windows patching (including Win 10→11 upgrades) via declarative policy
4. Monitor endpoint performance proactively with threshold alerting
5. Monitor controller health and receive alerts before issues impact operations

**Success Metric**: MSPs can achieve CMMC Level 1 or CIS Level 1 compliance across their fleet using CFGMS automation alone.

---

## Current State (Post v0.5.0)

### Completed Foundation

**v0.5.0 Achievements** ✅:
- MQTT+QUIC hybrid communication (Stories 12.1-12.5)
- Multi-tenant isolation with 3-tenant validation (Story 12.5)
- High availability controller clustering with 1.3s failover (Story 10.1)
- Advanced workflow engine with debugging (Stories 2.1-2.6)
- Git-based pluggable storage architecture (Epic 6)
- DNA collection system with 161 attributes (Story 78)
- Internal platform monitoring (Story 7.1)
- Configuration templates with basic functionality (Story 76)
- Basic script execution (Story 39)
- Configuration inheritance cascade (Story 37)

**Technology Stack**:
- **Language**: Go 1.21+
- **Communication**: MQTT+QUIC (mochi-mqtt + quic-go)
- **Storage**: Pluggable (Git/PostgreSQL/SQLite)
- **Architecture**: Controller + Steward + Outpost
- **Security**: mTLS, Zero-trust, RBAC with JIT access
- **Platform Support**: Windows (10/11/Server 2019/2022), Linux (Ubuntu/Debian/RHEL), macOS (Ventura/Sonoma)

### What's Missing (v0.6.0 Scope)

1. **Production-grade script management** - Versioning, DNA-based parameters, ephemeral API keys
2. **Compliance template marketplace** - CIS/CMMC Level 1 templates for Windows/Linux/macOS
3. **Enterprise Windows patching** - Policy-driven patching with major version upgrades (Win 10→11)
4. **Endpoint performance monitoring** - Time-series metrics, threshold alerting, workflow-triggered remediation
5. **Controller health monitoring** - Observability, alerting, and troubleshooting for small-medium deployments

---

## Story Breakdown

### Story 1: Advanced Script Execution Capabilities (13 points)

#### What's Being Added

**1. Script Versioning via Git Storage Provider**
- Semantic versioning (v1.0.0, v1.1.0, v2.0.0)
- Full git history and rollback capabilities
- Per-MSP git repository for script organization

**2. Advanced Parameter Handling**
- **Device DNA injection**: `$DNA.OS.Version`, `$DNA.Hardware.CPUCores`, `$DNA.Network.IPAddress`
- **Company DNA/properties**: `$CompanySettings.BackupPath`, `$TenantPolicy.RetentionDays`
- **Runtime parameters**: Ad-hoc parameter specification for one-time executions
- **Ephemeral API keys**: Controller generates time-limited tokens for script-to-controller callbacks

**3. Multi-Platform Script Support**
- PowerShell (.ps1) - Windows
- Bash/zsh (.sh) - Linux/macOS
- Python (.py) - All platforms
- Batch/cmd (.bat, .cmd) - Windows legacy

**4. Workflow Engine Integration**
- Immediate execution via workflow trigger
- Scheduled execution using workflow cron triggers
- Script dependencies handled by workflow steps
- Complex orchestration (pre-checks → execution → validation → rollback)

**5. Real-Time Execution Monitoring**
- **Aggregated view**: "87/100 completed, 13 running, 0 failed"
- **Drill-down**: Individual device stdout/stderr for troubleshooting
- **Real-time streaming**: Selected devices for live monitoring
- **Buffered logs**: Bulk executions available for playback
- **Failure alerting**: Real-time notifications on script failures

**6. Script Template Library**
- **Global templates**: System-provided scripts (CFGMS team maintains)
- **MSP repositories**: Per-MSP git repos (browse/organize like normal git)
- **Template discovery**: Import from global library into MSP repo
- **Version control**: Branching, tagging, merging per MSP

**7. Exit Code Handling**
- Capture and return exit codes to workflow engine
- Exit code-based conditional workflow logic
- No automatic retry logic (admin-controlled via workflows)

**8. Workflow Integration Patterns**
- Execute script as workflow step
- Pass workflow variables to script parameters
- Script output becomes workflow variables
- Error handling and rollback via workflow engine

#### How It Integrates
- Extends Story 39 (Basic Script Execution)
- Uses git storage provider for script versioning
- Leverages DNA system (Story 78) for parameter injection
- Integrates with workflow engine (Stories 2.1-2.6) for orchestration
- Uses RBAC for script execution permissions
- Respects multi-tenant isolation (Story 12.5)

#### Success Criteria
- [ ] Scripts versioned in git with semantic versioning
- [ ] DNA properties injectable as script parameters
- [ ] Ephemeral API keys generated for script callbacks
- [ ] Multi-platform execution (PowerShell/Bash/Python/batch)
- [ ] Workflow engine scheduling and orchestration
- [ ] Real-time monitoring for selected devices, aggregated for bulk
- [ ] Global template library with 10+ common scripts
- [ ] Per-MSP git repositories functional
- [ ] Exit codes captured and returned to workflows

---

### Story 2: Configuration Templates (Advanced) (13 points)

#### What's Being Added

**1. Bundled Compliance Templates**
- One template = complete compliance level (e.g., CIS Level 1 = 50+ controls)
- Consistent implementation across fleet
- Customizable via template inheritance/override

**2. Minimum Viable Compliance Template Set**
- **CIS Level 1 - Windows 10/11 Enterprise** (50+ controls)
- **CIS Level 1 - Windows Server 2019/2022** (50+ controls)
- **CIS Level 1 - macOS Ventura/Sonoma** (50+ controls)
- **CIS Level 1 - Ubuntu/Debian** (50+ controls)
- **CMMC Level 1 - Windows (all versions)** (17 practices)
- **Total: 5 core compliance templates**

**3. OSS Template Repository**
- GitHub-based template repository
- Community contribution via PR workflow
- CFGMS team maintains core compliance templates
- Semantic versioning for template updates

**4. Template Marketplace Discovery**
- Browse/search by framework (CIS/CMMC/NIST), OS, compliance level
- Template metadata (control count, last updated, framework version)
- Usage documentation and examples per template

**5. Template Testing Framework**
- Unit tests: Render template with mock DNA, validate output
- Test scenarios: Define input DNA + expected output config
- CI/CD validation for template PRs

**6. Compliance Validation** (leverage existing features)
- DNA + drift detection reports compliance status
- Template metadata tags controls for reporting
- Compliance dashboards: "87% CIS Level 1 compliant"

**7. Template Collaboration Workflows**
- Fork/clone templates from marketplace
- Customize for MSP-specific requirements
- Contribute back to community (optional)
- Version tracking and update notifications

#### Template Structure Example

```yaml
metadata:
  name: CIS Windows 10 Enterprise Level 1
  version: 2.0.0
  framework: CIS
  benchmark_version: v2.0.0
  level: 1
  os_family: Windows
  os_versions: ["Windows 10", "Windows 11"]
  control_count: 54
  last_updated: 2025-10-10
  maintainer: CFGMS Team
  license: Apache-2.0

# Uses existing Story 76 template syntax
modules:
  - name: password-policy
    type: registry
    controls: [CIS-1.1.1, CIS-1.1.2, CMMC-IA.L1-3.5.7]
    settings:
      PasswordComplexity: $if($DNA.OS.Edition == "Enterprise", 1, 0)
      MinimumPasswordLength: 14
      PasswordHistorySize: 24

  - name: disable-guest-account
    type: registry
    controls: [CIS-2.3.1.1, CMMC-AC.L1-3.1.1]
    settings:
      EnableGuestAccount: 0

  - name: enable-firewall
    type: firewall
    controls: [CIS-9.1.1, CMMC-SC.L1-3.13.1]
    profiles: [Domain, Private, Public]
    default_inbound: deny
    default_outbound: allow
```

#### How It Integrates
- Extends Story 76 (Basic Templates)
- Uses git storage provider for template repository
- Leverages DNA system for conditional sections
- Integrates with drift detection (Story 80) for compliance validation
- Uses configuration validation for template testing
- Respects configuration cascade (MSP → Client → Group → Device)

#### Success Criteria
- [ ] 5 core compliance templates available (CIS Level 1 + CMMC Level 1)
- [ ] OSS repository accepting community contributions
- [ ] Template marketplace browsable/searchable
- [ ] Template testing framework validates all templates
- [ ] Compliance reporting shows "X% CIS Level 1 compliant"
- [ ] MSPs can fork and customize templates
- [ ] Template versioning with semantic versioning

---

### Story 3: Windows Patch Compliance (Advanced) (13 points)

#### What's Being Added

**1. Policy-Driven Patch Management Engine**

**Declarative Patching Policy** (device/group/client/MSP config):
```yaml
windows_patching_policy:
  patch_types:
    critical:
      install_after_days: 7    # Install 7 days after Microsoft releases
      max_compliance_days: 14  # Must be installed within 14 days
    important:
      install_after_days: 14
      max_compliance_days: 30
    moderate:
      install_after_days: 30
      max_compliance_days: 60
    optional:
      install_after_days: 90
      max_compliance_days: 180

  feature_updates:
    target_version: "Windows 11 24H2"
    install_after_release_days: 90
    compatibility_checks: true

  maintenance_windows:
    - day: sunday
      start: "02:00"
      end: "05:00"
    honor_user_presence: true
    max_defer_days: 7

  compliance_alerting:
    warning_days_before_breach: 7
    critical_days_before_breach: 1
```

**CFGMS Enforcement**:
- Continuously monitors patch release dates (Microsoft Update Catalog)
- Automatically installs patches when `install_after_days` + maintenance window
- Proactively alerts when approaching `max_compliance_days`
- Enforces policy across all devices without manual intervention

**2. Windows Update COM API Integration**
- `IUpdateSession`, `IUpdateSearcher`, `IUpdateDownloader`, `IUpdateInstaller` COM interfaces
- Go ↔ COM interop via `github.com/go-ole/go-ole`
- Search, download, install patches locally on endpoints
- Filter patches by severity and release date

**3. Major Version Upgrade Policy Enforcement**

**Policy-Driven Upgrades**:
```yaml
feature_updates:
  target_version: "Windows 11 24H2"
  install_after_release_days: 90
  compatibility_checks: true
```

**CFGMS Behavior**:
1. Validates hardware compatibility (TPM 2.0, CPU, RAM, disk)
2. Waits 90 days after Microsoft releases 24H2
3. Automatically upgrades during maintenance window
4. Monitors DNA post-upgrade for validation
5. Reports completion/failure (Windows built-in rollback handles failures)

**Compatibility Validation** (DNA-based):
- TPM 2.0 present
- UEFI boot enabled
- CPU on Windows 11 compatibility list
- RAM ≥4GB
- Free disk space ≥64GB

**4. Maintenance Window Enforcement** (Generic System Feature)

**Check if already implemented** - If not, build as:
```yaml
# Cascading configuration (MSP → Client → Group → Device)
maintenance_windows:
  reboot_allowed:
    - day: sunday
      start: "02:00"
      end: "05:00"
    - day: wednesday
      start: "02:00"
      end: "04:00"

  honor_user_presence: true
  max_defer_days: 7
  emergency_override: false
```

**Enforcement**:
- ALL reboot operations check maintenance window (not just patching)
- Modules call `device.CanReboot()` before rebooting
- Defers reboot if outside window (up to `max_defer_days`)
- Forces reboot after `max_defer_days`

**5. Proactive Compliance Alerting**

**Alert Examples**:
- **Warning**: "Device DESKTOP-123 will breach patch compliance in 5 days (Critical patch KB5034441 not installed)"
- **Critical**: "Device SERVER-456 will breach compliance in 1 day (10 Important patches missing)"
- **Breach**: "Device LAPTOP-789 is NON-COMPLIANT (Critical patch KB5034441 not installed - 3 days overdue)"

**Alert Delivery**:
- CFGMS dashboard (when built)
- Workflow engine triggers (email, webhooks)
- MSP admin notification queue
- Per-tenant alert configuration

**6. Basic Compliance Reporting + Alerting**

**Report Contents**:
- Device name, OS version, patch level
- Missing patches (count by severity: Critical/Important/Moderate)
- Last patch date
- Compliance status: ✅ Compliant / ⚠️ Warning / 🔴 Non-Compliant
- Win 11 upgrade eligibility (Yes/No/Warning)
- Days until compliance breach (countdown)

#### How It Integrates
- Uses Windows Update COM API for local patch management
- Leverages workflow engine for orchestration
- Uses DNA system for compatibility validation
- Integrates with configuration rollback (Story 75)
- Uses multi-tenant isolation (Story 12.5)
- Integrates with reporting framework (Story 5.1)
- Uses pluggable storage for patch policy

#### Success Criteria
- [ ] MSPs define patch policy declaratively in configuration
- [ ] CFGMS automatically installs patches per policy
- [ ] Maintenance windows honored for all reboot operations (not just patching)
- [ ] Proactive alerting 7 days before compliance breach
- [ ] Critical alerting 1 day before compliance breach
- [ ] Major version upgrades (Win 10→11, 23H2→24H2) policy-driven
- [ ] Hardware compatibility validation blocks incompatible upgrades
- [ ] Basic compliance reporting shows device status + days until breach
- [ ] Windows Update COM API functional on all Windows versions
- [ ] Windows built-in rollback handles failed upgrades automatically

---

### Story 4: Endpoint Performance Monitoring (13 points)

#### What's Being Added

**1. Performance Metrics Collection** (Separate from DNA)

**DNA System** (existing - unchanged):
- Purpose: System "blueprint" (hardware specs, installed software)
- Change rate: Low (only during upgrades/installs)
- Storage: Relational database (Git/PostgreSQL/SQLite)
- Collection: Every 5 minutes (configurable)

**Performance Monitoring** (new):
- Purpose: System "vital signs" (resource utilization)
- Change rate: High (changes every second)
- Storage: Time-series database (InfluxDB/TimescaleDB)
- Collection: Every 60 seconds (configurable)

**Basic Resource Metrics**:
```yaml
performance_metrics:
  cpu:
    usage_percent: 73.2      # Overall CPU utilization

  memory:
    total_bytes: 17179869184
    used_bytes: 8796093022
    usage_percent: 51.2

  disk:
    read_bytes_per_sec: 15728640
    write_bytes_per_sec: 5242880
    usage_percent: 68.5

  network:
    bytes_sent_per_sec: 1048576
    bytes_received_per_sec: 2097152

  system:
    online_status: true
    last_checkin: "2025-10-10T14:32:15Z"
    uptime_seconds: 259200
```

**Top Resource Consumers**:
```yaml
top_processes:
  cpu:
    - name: "chrome.exe", pid: 4521, cpu_percent: 23.5
    - name: "sqlservr.exe", pid: 1234, cpu_percent: 18.2

  memory:
    - name: "chrome.exe", pid: 4521, memory_mb: 2048
    - name: "sqlservr.exe", pid: 1234, memory_mb: 1536
```

**2. Process and Service Monitoring**

**Watchlist Configuration**:
```yaml
process_monitoring:
  watched_processes:
    - name: "sqlservr.exe"
      alert_if_missing: true
      auto_start: false
      remediation_workflow: "restart-sql-server"

  watched_services:
    - name: "MSSQLSERVER"
      alert_if_stopped: true
      auto_start: false
      remediation_workflow: "sql-service-recovery"
```

**Behavior**:
- Monitor if specific processes/services exist/running
- Alert if missing/stopped
- Optionally attempt to start (if `auto_start: true`)
- Trigger workflow for complex remediation

**3. Threshold-Based Alerting**

**Alert Configuration**:
```yaml
performance_alerting:
  thresholds:
    cpu:
      warning: 80
      critical: 95
    memory:
      warning: 85
      critical: 95
    disk:
      warning: 90
      critical: 98

  alert_durations:
    warning: 300   # 5 minutes sustained
    critical: 120  # 2 minutes sustained
```

**4. Time-Series Storage Architecture**

**Separate Storage**:
- **DNA Storage**: Git/PostgreSQL/SQLite (point-in-time snapshots)
- **Performance Storage**: InfluxDB/TimescaleDB (continuous time-series)
- **Retention**: 30 days (default, configurable at global/MSP/company level)

**5. Workflow-Driven Remediation**

No automatic remediation - trigger workflows instead:
```yaml
workflow:
  name: SQL Server Recovery
  trigger: performance_alert

  steps:
    - verify-issue
    - notify-dba
    - wait-for-approval
    - restart-service
    - validate-recovery
```

**6. Multi-Platform Support** (Windows/Linux/macOS)

**Collection Methods**:
- **Windows**: PowerShell (`Get-Counter`, `Get-Process`)
- **Linux**: `/proc` filesystem (`/proc/stat`, `/proc/meminfo`)
- **macOS**: `sysctl`, `vm_stat`, `iostat`

**Implementation**:
- Extend existing DNA collector with performance metrics
- Platform-specific collectors (same pattern as Story 78)
- Unified metric format sent to controller

**7. Configuration Cascading**

```yaml
# Global default
performance_monitoring:
  enabled: true
  collection_interval_seconds: 60
  retention_days: 30

# MSP override
performance_monitoring:
  retention_days: 90

# Device override (critical server)
performance_monitoring:
  collection_interval_seconds: 10
  thresholds:
    cpu:
      critical: 90
```

#### How It Integrates
- Extends DNA collector mechanism (same code, different metrics)
- Uses time-series database (separate from DNA storage)
- Leverages workflow engine for remediation
- Uses MQTT for metrics streaming
- Integrates with alerting framework
- Respects multi-tenant isolation

#### Success Criteria
- [ ] Performance metrics collected every 60 seconds (configurable)
- [ ] Basic metrics: CPU, memory, disk I/O, network, online status
- [ ] Top 10 CPU/memory consumers tracked per device
- [ ] Process/service watchlist with alerting
- [ ] Threshold-based alerting (warning/critical levels)
- [ ] Time-series database storage (30-day retention, configurable)
- [ ] Workflow-triggered remediation (no automatic restarts)
- [ ] Multi-platform support (Windows/Linux/macOS)
- [ ] <1% CPU overhead on monitored endpoints
- [ ] DNA system unchanged (performance monitoring separate)

---

### Story 5: Controller Health Monitoring & Alerting (8 points)

**Scope**: Observability and alerting for small-medium deployments (10-1000 stewards)

#### What's Being Added

**1. Controller Performance Metrics Collection**

**MQTT Broker Metrics**:
- Active steward connections
- Message queue depth (pending messages)
- Message throughput (msgs/sec)

**Storage Provider Metrics** (existing Git/PostgreSQL/SQLite):
- Connection pool utilization
- Query latency (avg, p95)
- Slow query count (>1 second)
- Connection failures and retries

**Application Metrics**:
- **Workflow queue depth** (waiting workflows, max wait time)
- **Script execution queue depth** (waiting scripts, max wait time)
- Active workflow executions

**System Metrics** (per controller node):
- CPU utilization
- Memory usage (heap, RSS)
- Goroutine count

**Collection Interval**: 30 seconds (low overhead)
**Retention**: 7 days (sufficient for troubleshooting)

**2. Threshold-Based Alerting**

**Alert Triggers**:
- 🔴 **CRITICAL**: Workflow queue depth >50
- 🔴 **CRITICAL**: Script queue depth >50
- 🔴 **CRITICAL**: Storage query latency >2 seconds
- 🔴 **CRITICAL**: MQTT queue depth >100
- 🔴 **CRITICAL**: CPU >90% for 5 minutes
- 🔴 **CRITICAL**: Memory >90% for 5 minutes

**Alert Detection**: Check every 30 seconds
**Proactive Detection**: Alert BEFORE MSPs notice issues

**3. Email Alerting**

**Simple Email Alerts**:
- SMTP integration for email delivery
- Email template includes:
  - Issue description
  - Current metric value vs threshold
  - Timestamp
- Per-tenant email configuration (cascading)
- Rate limiting (max 1 email per issue per 15 minutes)

**No tiered escalation** - simple CRITICAL alerts only for alpha→beta

**4. Request Tracing**

**Distributed Tracing Lite**:
- Generate unique request ID for each operation
- Inject request ID into context
- Structured logging with request ID
- CLI tool: `cfgctl trace <request_id>` (grep logs, reconstruct path)

**Example Log Flow**:
```
[INFO] request_id=abc123 component=mqtt_handler latency=5ms
[INFO] request_id=abc123 component=storage latency=45ms
[INFO] request_id=abc123 component=workflow_engine latency=120ms
[INFO] request_id=abc123 total_latency=170ms status=success
```

**5. Health Check API**

**REST API Endpoints**:
- `GET /api/v1/health` - Simple up/down status
- `GET /api/v1/health/detailed` - Metrics + component health
- `GET /metrics` - Prometheus endpoint

**Response includes**:
- MQTT broker status (connections, queue depth)
- Storage provider status (connection pool, query latency)
- Queue depths (workflow, script)
- System metrics (CPU, memory, goroutines)

**6. CLI Status Tools**

**CLI Commands**:
- `cfgctl controller status` - Human-readable health summary
- `cfgctl controller metrics` - Detailed metrics view
- `cfgctl trace <request_id>` - Request flow reconstruction

**CLI Output Example**:
```bash
$ cfgctl controller status

Controller Health Status
Node: controller-01
Status: HEALTHY

MQTT Broker:
  Connections: 234 / 1,000 (23%)
  Queue Depth: 5

Storage Provider (PostgreSQL):
  Connection Pool: 12 / 20 (60%)
  Query Latency (avg): 25ms

Application:
  Workflow Queue: 3 (max wait: 2.1s)
  Script Queue: 1 (max wait: 0.8s)
  CPU: 34%
  Memory: 42%
```

#### How It Integrates
- Extends Story 7.1 (Internal Platform Monitoring)
- Monitors existing storage providers (Git/PostgreSQL/SQLite)
- Uses existing pluggable storage for metrics persistence
- Uses SMTP for email alerting
- Prometheus integration for existing monitoring stacks

#### Success Criteria
- [ ] Controller metrics collected every 30 seconds
- [ ] Existing storage providers monitored (Git/PostgreSQL/SQLite)
- [ ] MQTT broker and queue depth monitoring
- [ ] Threshold-based alerting on CRITICAL thresholds
- [ ] Email alerts sent when thresholds breached
- [ ] Request tracing via log correlation
- [ ] Health API endpoints (simple + detailed + Prometheus)
- [ ] CLI tools for operations (`cfgctl controller status`)
- [ ] 7-day metrics retention in existing storage
- [ ] <1% overhead on controller performance
- [ ] Load tested with 1k steward connections

---

## Cross-Story Integration Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Controller Cluster (HA)                       │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Story 5: Controller Health Monitoring                      │ │
│  │  Queue Monitoring → Alerting → Observability               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────────┐   │
│  │ Story 3:     │  │ Story 2:     │  │ Story 1:            │   │
│  │ Windows      │→ │ Compliance   │→ │ Script Execution    │   │
│  │ Patching     │  │ Templates    │  │ (DNA-parameterized) │   │
│  └──────────────┘  └──────────────┘  └─────────────────────┘   │
│         ↓                   ↓                    ↓               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │      MQTT+QUIC Communication (Multi-Tenant Isolated)      │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬──────────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────────┐
        │                       │                           │
┌───────▼─────────┐    ┌────────▼──────────┐      ┌────────▼─────────┐
│   Steward 1     │    │   Steward 2       │      │   Steward N      │
│   (Tenant A)    │    │   (Tenant B)      │      │   (Tenant C)     │
│                 │    │                   │      │                  │
│ ┌─────────────┐ │    │ ┌───────────────┐ │      │ ┌──────────────┐ │
│ │ Story 4:    │ │    │ │ Story 3:      │ │      │ │ Story 1 + 2  │ │
│ │ Performance │ │    │ │ Patching      │ │      │ │ Scripts +    │ │
│ │ Monitoring  │ │    │ │ + Story 4     │ │      │ │ Templates    │ │
│ └─────────────┘ │    │ └───────────────┘ │      │ └──────────────┘ │
└─────────────────┘    └───────────────────┘      └──────────────────┘
```

### Cross-Story Dependencies

**Story 1 → Story 2 → Story 3**:
- Templates (Story 2) generate configs referencing scripts (Story 1)
- Patching (Story 3) uses scripts for pre/post-upgrade tasks
- All use DNA system for dynamic parameterization

**Story 4 → Story 5**:
- Endpoint metrics (Story 4) aggregate to controller metrics (Story 5)
- Controller performance (Story 5) affects endpoint metric delivery
- Shared time-series database and alerting infrastructure

**Story 3 depends on all stories**:
- Story 1: Pre/post-upgrade scripts
- Story 2: Compliance validation templates
- Story 4: Performance monitoring during upgrades
- Story 5: Controller queue management during mass patching

---

## Risk Assessment & Mitigation

### High Risk: Windows Major Version Upgrades (Story 3)

**Risk**: Major upgrades (Win 10→11) could fail and brick devices
**Impact**: Critical - Device unusable, requires manual intervention
**Probability**: Medium (10-15% failure rate in enterprise)

**Mitigation**:
- DNA-based compatibility validation blocks incompatible devices
- Windows built-in rollback handles failures (<10 minutes recovery)
- Phased rollout via device groups (test → pilot → production)
- Automated pre-upgrade backups via workflow
- Emergency stop mechanism (pause all upgrades)

**Rollback Plan**:
- Windows Recovery Environment automatic rollback
- Maximum 10-minute recovery time (Windows built-in)
- Configuration snapshot restoration via git

---

### Low Risk: Controller Monitoring Overhead (Story 5)

**Risk**: Monitoring overhead degrades controller performance
**Impact**: Medium - Could affect small-medium deployments (10-1k stewards)
**Probability**: Very Low (<5% with 30s collection interval)

**Mitigation**:
- 30-second collection interval (low overhead)
- Simplified metrics (no high-cardinality metrics)
- 7-day retention only (minimal storage impact)
- Feature flag to disable monitoring if needed
- Load tested with 1k stewards

**Rollback Plan**:
- Disable health monitoring via config
- Fall back to Story 7.1 basic monitoring
- No impact on core controller functionality

---

### Medium Risk: Script Execution Security (Story 1)

**Risk**: Malicious scripts could compromise endpoints
**Impact**: Critical - Full device compromise
**Probability**: Low (with proper controls)

**Mitigation**:
- RBAC-based script approval workflow
- Audit logging for all script executions
- Script signature verification (future: code signing)
- Multi-tenant isolation (tenant scripts cannot cross boundaries)

**Rollback Plan**:
- Kill running scripts via MQTT command
- Revert configuration to pre-script state
- Quarantine compromised devices
- Comprehensive audit trail for forensics

---

### Low Risk: Template Complexity (Story 2)

**Risk**: Compliance templates too complex, leading to errors
**Impact**: Medium - Configuration errors could disable services
**Probability**: Medium (30-40% initial misconfiguration)

**Mitigation**:
- Community-tested templates from marketplace
- Template validation framework catches errors
- DNA-based conditional logic handles edge cases
- Configuration rollback (Story 75) handles failures

**Rollback Plan**:
- Git-based version control (instant revert)
- Automatic rollback on validation failure
- Manual rollback via cfgctl CLI

---

## Testing Strategy

### Unit Testing
- [ ] 90%+ test coverage for all new code
- [ ] Error paths tested with negative cases
- [ ] Race conditions tested with `-race` flag
- [ ] Cross-platform compatibility tests

### Integration Testing
- [ ] Story 1: Script execution across Windows/Linux/macOS
- [ ] Story 2: Template rendering with DNA conditionals
- [ ] Story 3: Windows patching in Docker test environment
- [ ] Story 4: Performance monitoring with real metrics
- [ ] Story 5: Controller health monitoring and alerting

### End-to-End Testing
- [ ] Full workflow: Template → Config → Script → Validation
- [ ] Multi-tenant isolation maintained across all stories
- [ ] Performance regression testing (v0.5.0 baseline vs v0.6.0)

### Load Testing
- [ ] Story 1: 1000 concurrent script executions
- [ ] Story 3: 500 simultaneous patch deployments
- [ ] Story 4: 10k endpoints reporting metrics
- [ ] Story 5: Controller with 1k connected stewards (alpha→beta target)

### Security Testing
- [ ] gosec security scanning (zero critical/high)
- [ ] Script execution RBAC bypass testing
- [ ] Input validation fuzzing
- [ ] Multi-tenant isolation validation

---

## Definition of Done

### Code Quality
- [ ] All 5 stories completed with acceptance criteria met
- [ ] Unit tests passing with 90%+ coverage
- [ ] Integration tests passing (no regressions)
- [ ] Security scans passing (zero critical/high)
- [ ] Code review approved by 2+ reviewers
- [ ] Linting passing (golangci-lint)

### Documentation
- [ ] Architecture documentation updated
- [ ] API documentation generated (Swagger/OpenAPI)
- [ ] User guides for all new features
- [ ] Developer guides for extending functionality
- [ ] Operational runbooks created
- [ ] Migration guide from v0.5.0 to v0.6.0

### Testing
- [ ] Existing functionality verified (no regressions)
- [ ] Integration points working correctly
- [ ] Load testing validates performance requirements
- [ ] Multi-tenant isolation verified
- [ ] Security testing passed

### Deployment
- [ ] Docker images built and tested
- [ ] Kubernetes manifests validated
- [ ] Database migrations tested (PostgreSQL/SQLite)
- [ ] Rollback procedures validated
- [ ] Monitoring configured

---

## Success Metrics

### Functional Metrics
- **Story 1**: 100+ script executions/day, 50+ templates in global library
- **Story 2**: 5 core compliance templates, 90%+ config from templates
- **Story 3**: 95%+ patch success rate, automated compliance enforcement
- **Story 4**: 100% endpoint coverage, anomaly detection <30 seconds
- **Story 5**: Performance issues detected before customer impact (100%)

### Performance Metrics
- Controller handles 1k+ concurrent steward connections (alpha→beta target)
- Script execution latency <5s (p95)
- Configuration validation <1s (p95)
- Template rendering <2s (p95)
- Monitoring overhead <1% CPU

### Operational Metrics
- System availability 99.9%+
- Mean time to detection (MTTD) <30 seconds
- Mean time to resolution (MTTR) <5 minutes (automated)
- Configuration rollback success rate 100%

---

## Milestone Timeline

### Phase 1: Foundation (Weeks 1-4)
- **Week 1-2**: Story 1 (Scripts) + Story 2 (Templates)
- **Week 3-4**: Story 3 (Patching)

### Phase 2: Monitoring (Weeks 5-7)
- **Week 5-6**: Story 4 (Endpoint Performance)
- **Week 7**: Story 5 (Controller Health Monitoring)

### Phase 3: Integration & Testing (Weeks 8-10)
- **Week 8**: Integration testing across all stories
- **Week 9**: Load testing and performance validation
- **Week 10**: Security testing and documentation

### Phase 4: Production Readiness (Weeks 11-12)
- **Week 11**: Production deployment preparation
- **Week 12**: v0.6.0 release and production rollout

**Total Timeline**: 12 weeks (3 months)

---

## Dependencies & Prerequisites

### Internal Dependencies (✅ Complete)
- v0.5.0 complete (all foundational capabilities)
- MQTT+QUIC validated (Story 12.1-12.5)
- Multi-tenant isolation tested (Story 12.5)
- HA infrastructure operational (Story 10.1)
- Workflow engine available (Stories 2.1-2.6)
- Storage architecture complete (Epic 6)
- DNA system operational (Epic 67)

### External Dependencies
- [ ] Time-series database (InfluxDB or TimescaleDB)
- [ ] Email server for alerting (SMTP)
- [ ] Git repository hosting (for template marketplace)

### Infrastructure Requirements
- PostgreSQL 14+ or SQLite 3.35+
- InfluxDB 2.x or TimescaleDB 2.x (new)
- Redis 7+ for caching (optional)

---

## Backlog Items for Future Versions

1. **Advanced Controller Management** (v0.7.0+, 5 points)
   - Automated self-healing (connection pool expansion, GC triggers)
   - Circuit breakers (database, external API, MQTT)
   - Admin-approved tenant migration across cluster nodes

2. **Module Development for Compliance Templates** (v0.7.0+, 13 points)
   - Registry module (Windows)
   - User/group module (all platforms)
   - Service module (all platforms)
   - Firewall module (all platforms)
   - Enables comprehensive CIS/CMMC template implementation

3. **Global Canary Ring System** (v0.7.0+, 8-13 points)
   - Device ring assignment (canary/pilot/production)
   - Automated ring progression
   - DNA-based health monitoring

4. **DEX Platform** (v1.6.0+, Phase 3)
   - Digital Employee Experience monitoring
   - User-centric performance metrics

5. **Performance Dashboards** (v0.7.0+)
   - Web UI for performance visualization
   - Real-time charts and graphs

6. **ML-Based Anomaly Detection** (v2.0+)
   - Dynamic baselines
   - Predictive alerting

7. **Advanced Integrations** (v0.7.0+)
   - PSA integrations (ConnectWise, Autotask)
   - PagerDuty, PagerTree on-call management
   - Slack, Microsoft Teams notifications

---

## Related Documentation

- [v0.5.0 Completion Status](./roadmap.md#v050-beta---advanced-workflows--core-readiness)
- [MQTT+QUIC Integration Testing](../testing/story-12.2-mqtt-quic-integration.md)
- [Multi-Tenant Isolation Testing](../../test/integration/mqtt_quic/MULTI_TENANT_TESTING.md)
- [Configuration Rollback System](./roadmap.md#story-75)
- [Template System](./roadmap.md#story-76)
- [HA Infrastructure](./roadmap.md#story-101)
- [Development Workflow](../development/story-checklist.md)

---

## Notes

- This epic completes **Phase 1: Foundation** (v0.1.0 - v0.6.0)
- After v0.6.0, project transitions to **v0.7.0 (Pre-OSS Preparation)**
- Story 5 (Controller Performance) is **HIGH PRIORITY** for MSP operations
- Story 3 (Windows Patching) has highest risk but highest MSP value
- All stories maintain backward compatibility with v0.5.0
- **Design Principle**: Configuration as policy, CFGMS as continuous enforcement

---

**Epic Owner**: TBD
**Technical Lead**: TBD
**Product Owner**: TBD
**Target Release**: Q2 2026 (estimated)

---

*Generated by BMad Master using brownfield epic creation methodology*
*Last Updated: 2025-10-13*
*Total Story Points: 60*
